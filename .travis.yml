language: node_js
node_js:
  - lts/*
install:
  - npm install # npm ci
script:
  - npm docs:build # npm run docs:build
deploy:
  provider: pages
  cleanup: false
  local_dir: docs/.vuepress/dist
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
